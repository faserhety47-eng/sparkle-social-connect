services:
  web:
    build: .
    container_name: smm-cat
    restart: unless-stopped
    ports:
      - "80:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
      HOST: 0.0.0.0
